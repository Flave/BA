const colors = require('colors');

module.exports.blue = (input) => {
  console.log(colors.blue(input));
}

module.exports.red = (input) => {
  console.log(colors.red(input));
}

module.exports.green = (input) => {
  console.log(colors.green(input));
}

module.exports.rainbow = (input) => {
  console.log(colors.rainbow(input));
}
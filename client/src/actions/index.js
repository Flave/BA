export const receiveLogin = (login) => ({
  type: 'RECEIVE_LOGIN',
  login
});

export const receivePredictions = (predictions) => ({
  type: 'RECEIVE_PREDICTIONS',
  predictions
});

export const receiveProfile = (profile) => ({
  type: 'RECEIVE_PROFILE',
  profile
});